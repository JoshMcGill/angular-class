//check if list is equal to one
//split list in half
//check if even or odd

function mergeSort(list) {
    if (list.length <= 1) {
        sortedList.push(list[0]);
        return;
    }
    
    var sortedList = []
    var halfLength = Math.ceil(arrayName.length / 2);
    
    for(var i = 0; i < halfLength; i + 2){
        if(halfLength(i))    
    }
    for(var i = 0; i < list; i++){
           
    }


}